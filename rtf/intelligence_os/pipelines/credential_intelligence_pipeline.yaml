name: credential_intelligence_pipeline
input: {type: email}
stages:
  - module: dehashed
    input: {query: email}
    required: false
  - module: leaklookup
    input: {query: email}
    required: false
output:
  entities: [credentials, domains]
graph:
  relationships: [email -> credential, credential -> domain]
